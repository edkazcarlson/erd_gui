#include "tableattribute.h"

TableAttribute::TableAttribute()
{
//TODO make it so that this can take 2 or 3 values, depending on how many are needed
//ex, varchar needs 3, name, varchar, length, but}
